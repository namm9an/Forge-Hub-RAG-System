# Shared environment variables for the monorepo
# Copy this to .env (do not commit .env) and set values as needed

# App
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=Advanced RAG Chatbot

# Backend
BACKEND_PORT=8000
BACKEND_HOST=127.0.0.1
ALLOWED_ORIGINS=http://localhost:3000
LOG_LEVEL=INFO

# Security
JWT_SECRET=changeme_in_dev_only

# External services (placeholders)
VECTOR_DB_URL=
VECTOR_DB_API_KEY=
EMBEDDING_API_URL=
EMBEDDING_API_KEY=

